apiVersion: v1
kind: Service
metadata:
  name: employee-directory-service
  namespace: employee-directory
spec:
  selector:
    app: employee-directory
  ports:
  - port: 80
    targetPort: 3000
  type: ClusterIP